# Arquivo de exemplo para configuração do ambiente de testes.
# Copie este arquivo para .env.test e preencha com os valores reais do SEU projeto Supabase de TESTE.
# NÃO FAÇA COMMIT do arquivo .env.test com as credenciais reais.

# URL do seu projeto Supabase de TESTE
SUPABASE_TEST_URL="https://your-test-project-ref.supabase.co"

# Chave de serviço (SERVICE_ROLE) do seu projeto Supabase de TESTE
# Use a chave SERVICE_ROLE para testes, pois ela permite bypassar RLS,
# o que é útil para configurar/limpar dados durante os testes.
SUPABASE_TEST_SERVICE_ROLE_KEY="your-test-service-role-key"

# Outras variáveis que podem ser necessárias para testes (ex: chaves de API para serviços mockados ou reais)
# OPENAI_API_KEY="sk-test-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# OPENWEATHERMAP_API_KEY="your-test-openweathermap-key" 